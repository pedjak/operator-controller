[project]
name = "operator-controller-e2e-tests"
version = "0.1.0"
description = "OLM end-to-end tests"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "behave==1.3.3",
    "polling2==0.5.0",
    "pyyaml==6.0.3",
    "ruff>=0.14.2",
]
